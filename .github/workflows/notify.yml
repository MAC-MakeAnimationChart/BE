name: Discord Notification

on:

  pull_request:
    types:
      - opened

jobs:
  discord:
    uses: MAC-MakeAnimationChart/.github/.github/workflows/discord-notify.yml@main
    secrets:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
